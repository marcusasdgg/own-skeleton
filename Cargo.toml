[package]
name = "own-skeleton"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]


[build-dependencies]
bindgen = "0.71.0"

[profile.release]
strip = "symbols"
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"

[dependencies]
spin = "0.10.0"
